# Copy to .env.mws.local and keep that file local only (gitignored).
# Point at any OpenAI-compatible upstream (example host below is a placeholder).

MWS_GPT_API_BASE=https://api.example.com/v1
MWS_GPT_API_KEY=replace-with-upstream-api-key
